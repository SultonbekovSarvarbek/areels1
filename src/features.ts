/**
 * Флаги незаконченных функций. Код остаётся в репозитории и компилируется —
 * прячем только вход в него, чтобы не выкатывать демо-режим покупателям.
 */

/**
 * Предложение цены и всё, что за ним тянется: вход по номеру, шторка оффера,
 * список предложений в профиле. Предложение пока никуда не уходит (хранится на
 * устройстве), а вход мокнут — код принимается любой. Включим, когда за этим
 * появится бэкенд.
 */
export const OFFERS_ENABLED = false;
